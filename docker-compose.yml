services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: aoe_league
      POSTGRES_USER: aoe
      POSTGRES_PASSWORD: aoe
    ports:
      - "5432:5432"
    volumes:
      - aoe_postgres:/var/lib/postgresql/data

volumes:
  aoe_postgres:
